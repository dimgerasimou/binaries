# trackpad-toggle

trackpad-toggle is a utility that ... 
toggles a laptop's trackpad on or off.

> Note: This only works on X11.

## Installation

To install simply edit the `Makefile` to
match your configuration and run:

```bash
make install
```

### Dependencies
To build requires the libraries `libxi`
and `libx11`
